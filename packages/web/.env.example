# Frontend-only flags. Never put secrets (e.g. GOOGLE_CLIENT_SECRET) here —
# anything prefixed VITE_ is bundled into the browser build.

# Flip to "true" once the backend /api/auth/google endpoint is live.
VITE_ENABLE_GOOGLE_OAUTH=false
